<%= ERB.new(File.read(File.expand_path("_header.erb",File.dirname(file)))).result(binding) -%>

# Options for ntpdate
OPTIONS="<%= @ntpdate_options %>"

# Set to 'yes' to sync hw clock after successful ntpdate
<% if @sync_hwclock -%>
SYNC_HWCLOCK=yes
<% else -%>
SYNC_HWCLOCK=no
<% end -%>
